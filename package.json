{
  "scripts": {
    "dev": "ts-node ./script.ts",
    "seed": "ts-node ./prisma/seed.ts",
    "clear:migrations": "rm -r ./prisma/migrations && npx prisma migrate reset --skip-seed",
    "migrate": "npx prisma migrate dev"
  },
  "devDependencies": {
    "prisma": "^4.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0"
  }
}
